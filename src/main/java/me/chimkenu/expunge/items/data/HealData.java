package me.chimkenu.expunge.items.data;

public class HealData {
    public enum Type {
        PERMANENT,
        TEMPORARY,
        BOOST,
        REVIVE
    }

    public enum Modifier {
        ABSOLUTE,
        RELATIVE
    }
}
